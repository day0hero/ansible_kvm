[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
retry_files_enabled = False

[connection]
pipelining = True

[privilege_escalation]
become = false
become_method = sudo
